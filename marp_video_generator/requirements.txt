# Marp Video Generator - Requirements
# =====================================
# Marp-based slide video generation workflow

# Core AI
google-generativeai>=0.8.0

# PDF Processing
pdf2image>=1.16.0

# Video Processing
moviepy>=1.0.3
imageio>=2.34.0
imageio-ffmpeg>=0.4.9

# Audio Processing
pydub>=0.25.1

# Image Processing
Pillow>=10.0.0
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Note: External tools required (must be installed separately):
# - marp-cli: npm install -g @marp-team/marp-cli
# - poppler-utils: apt-get install poppler-utils (Linux) / brew install poppler (Mac)
# - ffmpeg: apt-get install ffmpeg (Linux) / brew install ffmpeg (Mac)
