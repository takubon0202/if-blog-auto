# Jekyll Configuration for if(塾) Blog
title: if(塾) Blog
description: 教育・心理学・テクノロジーに関する最新情報を、科学的根拠に基づいてお届けします。
url: "https://takubon0202.github.io"
baseurl: "/if-blog-auto"

# Author
author:
  name: if(塾)

# Build settings
markdown: kramdown
highlighter: rouge
permalink: /:year/:month/:day/:title/

# Plugins (GitHub Pages supported)
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# Collections
collections:
  posts:
    output: true
    permalink: /posts/:year/:month/:day/:title/

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "AI Blog Generator"

# Exclude from build
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor

# Japanese language settings
lang: ja

# Pagination
paginate: 10
paginate_path: "/page/:num/"

# SEO
twitter:
  card: summary
